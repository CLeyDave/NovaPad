<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Easing Functions (must be defined before usage) -->
    <CircleEase x:Key="SmoothEaseOut" EasingMode="EaseOut"/>
    <CircleEase x:Key="SmoothEaseIn" EasingMode="EaseIn"/>
    <QuadraticEase x:Key="SmoothEaseInOut" EasingMode="EaseInOut"/>

    <!-- Sidebar Slide Animation -->
    <Storyboard x:Key="SidebarExpand">
        <DoubleAnimation Storyboard.TargetProperty="Width"
                         From="60" To="220" Duration="0:0:0.25"
                         EasingFunction="{StaticResource SmoothEaseOut}"/>
    </Storyboard>

    <Storyboard x:Key="SidebarCollapse">
        <DoubleAnimation Storyboard.TargetProperty="Width"
                         From="220" To="60" Duration="0:0:0.25"
                         EasingFunction="{StaticResource SmoothEaseOut}"/>
    </Storyboard>

    <!-- Fade In -->
    <Storyboard x:Key="FadeIn">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.3"
                         EasingFunction="{StaticResource SmoothEaseOut}"/>
    </Storyboard>

    <!-- Fade Out -->
    <Storyboard x:Key="FadeOut">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="1" To="0" Duration="0:0:0.2"
                         EasingFunction="{StaticResource SmoothEaseOut}"/>
    </Storyboard>

    <!-- Slide In From Right -->
    <Storyboard x:Key="SlideInFromRight">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                         From="50" To="0" Duration="0:0:0.35"
                         EasingFunction="{StaticResource SmoothEaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.35"
                         EasingFunction="{StaticResource SmoothEaseOut}"/>
    </Storyboard>

    <!-- Scale In -->
    <Storyboard x:Key="ScaleIn">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                         From="0.95" To="1" Duration="0:0:0.25"
                         EasingFunction="{StaticResource SmoothEaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                         From="0.95" To="1" Duration="0:0:0.25"
                         EasingFunction="{StaticResource SmoothEaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.25"/>
    </Storyboard>

    <!-- Pulse Animation -->
    <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever" AutoReverse="True">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0.6" To="1" Duration="0:0:0.8"
                         EasingFunction="{StaticResource SmoothEaseInOut}"/>
    </Storyboard>

    <!-- Glow Pulse (for RGB preview) -->
    <Storyboard x:Key="GlowPulse" RepeatBehavior="Forever" AutoReverse="True">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0.3" To="0.8" Duration="0:0:1.0"
                         EasingFunction="{StaticResource SmoothEaseInOut}"/>
    </Storyboard>

    <!-- Notification Slide In -->
    <Storyboard x:Key="NotificationSlideIn">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                         From="400" To="0" Duration="0:0:0.4"
                         EasingFunction="{StaticResource SmoothEaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.3"/>
    </Storyboard>

</ResourceDictionary>
